const name = " Daniel  ";
const trimName = name.trim()
let length = trimName.length;
let message = `My name is ${trimName} and the length of my name is: ${length} characters long.`;
console.log(message);