// Start by creating a variable `message`
var name = "Daniel";
let length = name.length;

let message = `My name is ${name} and the length of my name is: ${length} characters long.`;
console.log(message);